# Required: create a key at dashboard.sarvam.ai and keep it server-side.
SARVAM_API_KEY=replace_with_your_key

# Optional operational limits (safe defaults are used when omitted).
MAX_INPUT_CHARACTERS=2000
TRANSLATION_TIMEOUT_MS=25000
SPEECH_TIMEOUT_MS=45000
TRANSCRIPTION_TIMEOUT_MS=40000
